$(document).ready(function() {
    $("#startR").on('click', function(e) {
        e.preventDefault();
        // capture the input box text and begin a recording state
        // next go to the live_recording.html
    });
});